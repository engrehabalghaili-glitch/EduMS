import { Injectable } from '@angular/core';
import { SettingsDataSource } from './settings.datasource';

@Injectable()
export class SettingsApiDataSource extends SettingsDataSource {
  // TODO: Implement when backend API is ready
}
